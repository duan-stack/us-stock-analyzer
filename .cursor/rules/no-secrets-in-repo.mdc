---
description: 不要把 IP、口令、密钥写进仓库
alwaysApply: true
---

# 敏感信息不入库

不要把下列内容写入会提交或公开发布的文件（含 README、脚本、示例配置、注释）：

- 公网或内网 IP、真实服务器地址
- API Key、Token、数据库密码
- 带本机用户名的绝对路径

额外 CORS 来源用环境变量 `CORS_ORIGINS`，不要把生产 IP 写进代码。这个仓库是 GitHub 公开仓库，推送前必须检查 diff。
